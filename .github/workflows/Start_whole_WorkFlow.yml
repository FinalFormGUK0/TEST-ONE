name: Start whole WorkFlow

on:
  workflow_dispatch:    # manual trigger

permissions:
  actions: write
  contents: read

jobs:
  workflow-initiator:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6 hours timeout

    steps:
      - name: Start RDP Workflow
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          echo "🚀 Webhook-triggered RDP start workflow initiated"
          
          # Function to send Telegram notification
          send_telegram_notification() {
              local message="$1"
              local max_retries=3
              local retry_count=0
              
              if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
                  echo "⚠️ Telegram credentials not configured, skipping notification"
                  return 1
              fi
              
              while [ $retry_count -lt $max_retries ]; do
                  echo "📱 Attempt $((retry_count + 1)): Sending Telegram notification"
                  
                  response=$(curl -s -w "%{http_code}" -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
                       -d "chat_id=${TELEGRAM_CHAT_ID}" \
                       -d "text=${message}" \
                       -d "parse_mode=Markdown" \
                       -d "disable_web_page_preview=true")
                  
                  http_code="${response: -3}"
                  
                  if [ "$http_code" = "200" ]; then
                      echo "✅ Telegram notification sent successfully"
                      return 0
                  else
                      echo "❌ Failed to send notification (HTTP: $http_code)"
                      if [ $retry_count -lt $((max_retries - 1)) ]; then
                          echo "⏳ Retrying in 3 seconds..."
                          sleep 3
                      fi
                  fi
                  retry_count=$((retry_count + 1))
              done
              
              echo "❌ Failed to send Telegram notification after $max_retries attempts"
              return 1
          }
          
          # Function to trigger Call RDP email workflow
          trigger_call_rdp_email() {
              echo "🔁 Triggering Call RDP email workflow..."
              
              # Get the latest Call RDP email workflow run
              run_id=$(gh run list \
                --workflow "Call RDP email" \
                --repo "${{ github.repository }}" \
                --branch "${{ github.ref_name }}" \
                --limit 1 \
                --json databaseId \
                --jq ".[0].databaseId")

              if [ "$run_id" != "null" ] && [ -n "$run_id" ]; then
                  echo "🔁 Rerunning Call RDP email run ID: $run_id"
                  if gh run rerun $run_id --repo "${{ github.repository }}"; then
                      echo "✅ Call RDP email workflow triggered successfully"
                      send_telegram_notification "🚀 *RDP Workflow Started*\\n\\n✅ Call RDP email workflow triggered\\n✅ RDP server setup initiated\\n\\n📧 *Next:* Wait for email with RDP credentials\\n🔗 *Monitor:* [GitHub Actions](https://github.com/${{ github.repository }}/actions)"
                      return 0
                  else
                      echo "❌ Failed to trigger Call RDP email workflow"
                      send_telegram_notification "❌ *RDP Workflow Failed*\\n\\n❌ Failed to trigger Call RDP email workflow\\n\\n🔧 *Action:* Check [GitHub Actions logs](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})\\n🔄 *Fallback:* Trying alternative startup method..."
                      return 1
                  fi
              else
                  echo "❌ No Call RDP email workflow found to rerun"
                  send_telegram_notification "❌ *RDP Workflow Not Found*\\n\\n⚠️ No Call RDP email workflow available\\n\\n🔧 *Solution:* Try running workflows manually from [GitHub Actions](https://github.com/${{ github.repository }}/actions)"
                  return 1
              fi
          }
                    
          echo "⏳ Waiting for start_rdp message (max 6h)..."
          
          MAX_WAIT=$((360 * 60 - 300))  # wait 5h55m (leave 5 min buffer before GitHub kills job)
          INTERVAL=60                   # check every 1 min
          waited=0
          message_received=false
          
          while [ $waited -lt $MAX_WAIT ]; do
              # Poll Telegram for /start_rdp command
              updates=$(curl -s "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getUpdates?offset=-1")
              
              if echo "$updates" | grep -q "\"text\":\"/start_rdp\""; then
                  echo "🚀 /start_rdp command detected from Telegram!"
                  message_received=true
                  break
              fi
          
              echo "⏳ Still waiting... ($waited seconds elapsed)"
              sleep $INTERVAL
              waited=$((waited + INTERVAL))
          done



          
          if [ "$message_received" = true ]; then
              echo "✅ Triggering Call RDP email workflow..."
              run_id=$(gh run list \
                --workflow "Call RDP email" \
                --repo "${{ github.repository }}" \
                --branch "${{ github.ref_name }}" \
                --limit 1 \
                --json databaseId \
                --jq ".[0].databaseId")
          
              if [ "$run_id" != "null" ] && [ -n "$run_id" ]; then
                  echo "🔁 Rerunning Call RDP email run ID: $run_id"
                  gh run rerun $run_id --repo "${{ github.repository }}"
              else
                  echo "❌ No Call RDP email workflow found to rerun"
              fi
          else
              echo "⚠️ No start_rdp received before timeout, triggering on.yml fallback..."
              run_id=$(gh run list \
                --workflow "on" \
                --repo "${{ github.repository }}" \
                --branch "${{ github.ref_name }}" \
                --limit 1 \
                --json databaseId \
                --jq ".[0].databaseId")
          
              if [ "$run_id" != "null" ] && [ -n "$run_id" ]; then
                  echo "🔁 Rerunning on workflow run ID: $run_id"
                  gh run rerun $run_id --repo "${{ github.repository }}"
                  echo "✅ on.yml workflow rerun triggered"
              else
                  echo "❌ No on.yml workflow found to rerun"
              fi
          fi


     
